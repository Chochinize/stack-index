apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: stack-index

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# CI rewrites `newTag` to the built commit SHA (see .github/workflows/build.yml).
images:
  - name: chochinize/stack-index
    newTag: 1156b6da728f352d58eafbe31343bfc56bc69345
